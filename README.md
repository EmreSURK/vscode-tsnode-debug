With these settings, VSCode can run and debug a node app that is coded with TypeScript without compiling it to JavaScript.

    npm install ts-node --save-dev
    npm install typescript --save-dev

Enjoy! 
